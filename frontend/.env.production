# Production Backend Server URL
# If reverse proxied under the same domain, you can set this to empty or root: VITE_BACKEND_URL=
# Otherwise, set it to the public IP/domain of your production API:
VITE_BACKEND_URL=http://127.0.0.1:8000
